package:
  name: capsul

soma-dev:
  internal-dependencies:
    - soma-python

  type: interpreted

requirements:
  run:
    - pyyaml

tests:
  - script: "python -m capsul.test"
    requirements:
      run:
        - pytest
        - notebook
